# Domain Docs

Layout: **single-context**.

This repo currently has no root `CONTEXT.md` and no `docs/adr/` directory. Use `docs/architecture.md` as the canonical architecture reference; product scope lives in the versioned PRD files under `docs/prd/` (M1 uses `2026-09-01-zhiyan-m1.md` and its delivery backlog). GitHub Issues are the sole authority for ticket number, scope, dependency and lifecycle; use `gh` CLI per `docs/agents/issue-tracker.md`.
